sudo: required
language: php
php:
  - 5.5
  - hhvm

services:
  - docker

before_install:
  - docker build -t chanakar/apache-server .
  - docker run -d chanakar/apache-server
  - docker ps

script: phpunit Test.php
